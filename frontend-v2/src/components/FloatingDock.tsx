"use client";

/**
 * FloatingDock compatibility stub.
 * All navigation (workspace switcher, context navigation, and live counts)
 * is now unified into TopNav as the single source of truth.
 */
export default function FloatingDock() {
  return null;
}
